<% for column in Paramset.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @paramset.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @paramset %> |
<%= link_to 'Back', :action => 'list' %>
